{
  "name": "webapp-to-finish",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm install --legacy-peer-deps && npm start",
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "start": "cd frontend && npm start"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "serverless": "^2.0.0"
  }
}
